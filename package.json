{
  "name": "mathilde",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint --no-error-on-unmatched-pattern '**/*.js'",
    "lint:fix": "eslint --fix --no-error-on-unmatched-pattern '**/*.js'",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.5.0",
    "firebase-tools": "^15.20.0",
    "globals": "^17.6.0",
    "prettier": "^3.8.4"
  }
}
